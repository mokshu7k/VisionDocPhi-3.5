# Google Colab Optimized Requirements
# IMPORTANT: Colab comes with many pre-installed packages. This file avoids conflicts with them.

# PyTorch (pre-installed on Colab - comment out to use Colab's version)
# torch==2.12.0
# torchvision==0.27.0
# torchaudio==2.11.0

# Core ML/Vision packages (compatible with Colab's pre-installed versions)
transformers==4.40.0
huggingface_hub==0.19.0
pillow==10.1.0

# Numerical/ML packages (use versions compatible with Colab's pre-installed packages)
numpy==1.24.3
scipy==1.11.4
scikit-learn==1.3.2
tqdm==4.66.1

# Optional ML packages
gradio==4.26.0
diffusers==0.25.1
peft==0.7.1
datasets==2.17.0
accelerate==0.27.1

# Quantization (check if bitsandbytes is already installed)
bitsandbytes>=0.41.0

# Optional: Uncomment only if you have CUDA build tools available
# flash-attn>=2.0.0

# Kaggle-Optimized Requirements - MINIMAL VERSION
# PyTorch and CUDA are pre-installed in Kaggle - DO NOT INCLUDE torch/torchvision/torchaudio
# numpy, pandas, scikit-learn are pre-installed - DO NOT INCLUDE
# CRITICAL: scipy version MUST be specified to ensure compatibility with numpy
# Use --no-build-isolation flag when installing: pip install -q -r requirements_kaggle.txt --no-build-isolation

# ============================================================================
# ONLY the packages needed for VisionDocPhi-3.5 DocVQA
# ============================================================================

# Transformers and HuggingFace (core ML library)
transformers>=4.35.0
huggingface-hub>=0.22.0

# CRITICAL: scipy version compatibility fix
# scipy 1.17+ has compatibility issues with numpy 2.4.6 in Kaggle
# Use scipy 1.14.x which is stable and compatible across numpy versions
scipy>=1.11.0,<=1.14.1

# Image processing (may already be installed, but ensure compatible versions)
pillow>=10.0.0
opencv-python>=4.8.0

# Model optimization
peft>=0.7.0
accelerate>=0.27.0

# Quantization support (optional - code handles if unavailable)
bitsandbytes>=0.41.0

# Progress bars
tqdm>=4.66.0

# Optional: For data handling if needed
datasets>=2.17.0

# Optional: For creating interfaces
gradio>=4.20.0

# ============================================================================
# DO NOT INCLUDE - These are pre-installed in Kaggle:
# ============================================================================
# - torch, torchvision, torchaudio (pre-installed)
# - numpy, pandas, scipy (pre-installed)
# - scikit-learn, scikit-image (pre-installed)
# - tensorflow, keras (pre-installed but not needed)
# - jupyter-server, jupyter (pre-installed)
# - google-colab (pre-installed)
#
# These cause conflicts if included and are not needed for DocVQA

# ============================================================================
# INSTALLATION COMMAND FOR KAGGLE:
# ============================================================================
# !pip install -q -r requirements_kaggle.txt --no-build-isolation
# 
# If dependency conflicts appear, they are likely from Kaggle's pre-installed
# packages and can usually be safely ignored for this project.

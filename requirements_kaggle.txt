# Kaggle-Optimized Requirements
# PyTorch and CUDA are pre-installed in Kaggle - DO NOT INCLUDE torch/torchvision/torchaudio
# Use --no-build-isolation flag when installing: pip install -q -r requirements_kaggle.txt --no-build-isolation

# ============================================================================
# Core ML & Vision Libraries (Pre-built wheels only)
# ============================================================================
transformers>=4.35.0
huggingface-hub>=0.22.0
pillow>=10.1.0
opencv-python>=4.8.0

# ============================================================================
# Numerical/ML Packages (Compatible versions with pre-built wheels)
# ============================================================================
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
tqdm>=4.66.0

# ============================================================================
# Model Optimization & Quantization
# ============================================================================
# PEFT for parameter-efficient fine-tuning
peft>=0.7.0

# Bitsandbytes for 8-bit quantization (optional - will attempt, but code handles failure)
# For Kaggle, use: pip install bitsandbytes-cuda12x if this fails
bitsandbytes>=0.41.0

# ============================================================================
# Data & Training Utilities
# ============================================================================
datasets>=2.17.0
accelerate>=0.27.0

# ============================================================================
# Inference & Serving
# ============================================================================
gradio>=4.20.0
diffusers>=0.24.0

# ============================================================================
# Additional Utilities
# ============================================================================
scikit-image>=0.22.0
jsonlines>=4.0.0

# ============================================================================
# INSTALLATION NOTES FOR KAGGLE
# ============================================================================
# IMPORTANT: Install with this command:
#   !pip install -q -r requirements_kaggle.txt --no-build-isolation
#
# If bitsandbytes fails, use one of these alternatives:
#   !pip install bitsandbytes-cuda12x --no-build-isolation
#   OR skip it and the code will use float16 fallback:
#   !pip install -q --no-build-isolation -r requirements_kaggle.txt; true
#
# DO NOT include torch/torchvision/torchaudio - they're pre-installed in Kaggle
# DO NOT try to install flash-attn in Kaggle - use eager attention mode instead
